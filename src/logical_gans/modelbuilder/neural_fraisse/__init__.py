"""Neural Fraïssé experimental spine.

A learned adversarial model-building game over a generated task family:
the (active, symbolic) Devil chooses bounded challenges, the Builder replies with
semantic edits, a Judge checks progress. Builders are compared (uniform /
obligation-first / neural-passive / neural-active) on HELD-OUT tasks.

This is an internal experiment, NOT product/public surface.
"""
